# @memohjs/af

Meta package for Autofish CLI.

Install:

```bash
npm i -g @memohjs/af
```

The installer resolves your platform package automatically.

After install, run:

```bash
af --help
```

Optional shell completion:

```bash
af completion <bash|zsh|fish|powershell|elvish>
```
